# Docker镜像同步平台架构和逻辑分析

## 1. 核心架构概览

### 1.1 系统组成
- **后端**: Go + Gin框架
- **前端**: Vue.js 3 + Element Plus
- **数据库**: MySQL + GORM
- **版本控制**: Git集成(Gitee/GitHub)
- **CI/CD**: GitHub Actions
- **目标仓库**: 阿里云容器镜像服务(ACR)

### 1.2 核心服务层
```
├── handlers/          # HTTP请求处理器
│   └── sync.go        # 镜像同步处理器
├── services/          # 业务服务层
│   ├── git.go         # Git版本控制服务
│   ├── git_factory.go # Git服务工厂
│   ├── github.go      # GitHub API集成
│   └── config.go      # 配置管理服务
└── models/            # 数据模型
    └── models.go      # 数据模型定义
```

## 2. 镜像同步核心流程

### 2.1 同步请求处理流程
```
用户请求 → API接口 → 任务创建 → Git提交 → GitHub Actions → 状态监控 → 结果验证
```

#### 2.1.1 同步请求类型
- **单个同步** (`/api/v1/sync/submit`)
  - 顺序处理镜像
  - MaxConcurrent = 1
  - 适用于少量镜像快速同步

- **批量同步** (`/api/v1/sync/batch`)
  - 支持并发控制(1-10个并发)
  - 自定义重试策略
  - 优先级管理
  - 适用于大规模镜像迁移

### 2.2 任务处理详细流程

#### 步骤1: 任务创建和初始化
```go
// 生成唯一任务ID
taskID := uuid.New().String()

// 创建任务记录
task := &models.SyncTask{
    TaskID:        taskID,
    Status:        models.TaskStatusPending,
    MaxConcurrent: maxConcurrent,
    TotalImages:   len(images),
    AutoRetry:     autoRetry,
    RetryCount:    retryCount,
}
```

#### 步骤2: 镜像记录创建
```go
// 为每个镜像创建独立记录
record := &models.ImageSyncRecord{
    TaskID:        taskID,
    OriginalImage: originalImage,
    Tag:           tag,
    Architecture:  architecture,
    SyncStatus:    models.SyncStatusPending,
    InputOrder:    inputOrder,
    Priority:      priority,
    MaxRetries:    maxRetries,
}
```

#### 步骤3: 异步任务执行
```go
// 启动异步处理，避免HTTP阻塞
go h.processSyncTask(taskID)
```

## 3. Git集成和版本控制

### 3.1 Git服务工厂模式
```go
type GitServiceFactory struct {
    giteeService      *GitService        // Gitee服务实例
    githubService     *GitService        // GitHub服务实例
    encryptionService *EncryptionService // 加密服务
    mutex             sync.RWMutex       // 读写锁保护
    configCache       string             // 配置缓存
}
```

### 3.2 动态仓库选择
- **配置驱动**: 通过数据库配置选择仓库类型
- **支持平台**: Gitee + GitHub
- **无缝切换**: 运行时动态切换Git仓库
- **配置缓存**: 避免频繁数据库查询

### 3.3 Git操作流程

#### 3.3.1 仓库初始化
```
检查本地仓库 → 存在则打开 → 不存在则克隆 → 配置认证信息
```

#### 3.3.2 镜像文件更新
```go
func (s *GitService) UpdateImagesFile(newImages []string) (string, error) {
    // 1. 拉取最新代码
    if err := s.pullLatest(); err != nil {
        // 2. 失败时重新初始化
        s.CleanRepository()
        s.InitRepository()
    }

    // 3. 读取现有images.txt
    existingImages, _ := s.readImagesFile(imagesFilePath)

    // 4. 注释化历史记录
    commentedImages := make([]string, len(existingImages))
    for i, image := range existingImages {
        if !strings.HasPrefix(image, "#") {
            commentedImages[i] = "# " + image
        }
    }

    // 5. 合并新镜像和历史记录
    allImages := append(newImages, commentedImages...)

    // 6. 提交并推送
    commitSHA, _ := s.commitAndPush(newImages)

    return commitSHA, nil
}
```

#### 3.3.3 认证机制
- **Token认证**: 优先使用访问令牌
- **用户名密码**: Gitee支持传统认证
- **加密存储**: 敏感信息加密保存

## 4. GitHub Actions集成

### 4.1 工作流监控服务
```go
type GitHubService struct {
    client  *resty.Client
    baseURL string
    owner   string
    repo    string
}
```

### 4.2 监控流程
```
提交代码 → 等待30秒 → 查找工作流 → 获取运行ID → 定期检查状态 → 更新数据库
```

#### 4.2.1 工作流查找
```go
func (s *GitHubService) GetWorkflowRun(commitSHA string) (string, string, error) {
    maxRetries := 10
    for i := 0; i < maxRetries; i++ {
        // 查询匹配的工作流运行
        resp, err := s.client.R().
            SetQueryParam("head_sha", commitSHA).
            Get(url)

        // 查找匹配的运行
        for _, run := range response.WorkflowRuns {
            if run.HeadSHA == commitSHA {
                return fmt.Sprintf("%d", run.ID), run.HTMLURL, nil
            }
        }

        // 等待重试
        time.Sleep(30 * time.Second)
    }
}
```

#### 4.2.2 状态监控
- **监控时长**: 最多30分钟
- **检查间隔**: 30秒
- **状态类型**: queued, in_progress, completed
- **结论类型**: success, failure, cancelled, timed_out

## 5. 数据模型设计

### 5.1 核心实体关系
```
SyncTask (任务)
├── ImageSyncRecord[] (镜像记录)
├── GitHubActionURL (工作流链接)
├── CommitSHA (提交SHA)
└── Status (任务状态)
```

### 5.2 镜像同步记录
```go
type ImageSyncRecord struct {
    ID            uint       // 主键
    OriginalImage string     // 源镜像地址
    ACRImage      string     // 目标ACR地址
    Tag           string     // 镜像标签
    Architecture  string     // 架构(amd64/arm64)
    SyncStatus    string     // 同步状态
    TaskID        string     // 关联任务ID
    Priority      int        // 优先级
    RetryCount    int        // 重试次数
    StartedAt     *time.Time // 开始时间
    CompletedAt   *time.Time // 完成时间
    Duration      int64      // 耗时(秒)
    ImageSize     int64      // 镜像大小
}
```

### 5.3 状态流转
```
pending → syncing → success/failed
failed → retrying → success/failed
任何状态 → skipped
```

## 6. 并发控制和重试机制

### 6.1 并发控制策略
- **批量同步**: 1-10个并发可配置
- **模拟同步**: 使用信号量控制并发数
- **资源保护**: 防止系统过载

### 6.2 重试机制
```go
// 推送重试策略
func (s *GitService) pushWithRetry(commitSHA string) error {
    maxRetries := 3
    for i := 0; i < maxRetries; i++ {
        // 尝试推送
        err := s.repo.Push(&git.PushOptions{Auth: auth})
        if err == nil {
            return nil
        }

        // 重新同步
        s.pullLatest()

        // 递增等待时间
        time.Sleep(time.Duration(i+1) * time.Second)
    }
}
```

## 7. 镜像验证机制

### 7.1 ACR镜像验证
```go
func (h *SyncHandler) checkImageExistsInRegistry(imageRef string) bool {
    // 解析镜像引用
    ref, err := name.ParseReference(imageRef)
    if err != nil {
        return false
    }

    // 30秒超时检查
    ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
    defer cancel()

    // HEAD请求检查存在性
    _, err = remote.Head(ref, remote.WithContext(ctx))
    return err == nil
}
```

### 7.2 成功处理流程
```
GitHub Actions完成 → 验证每个镜像 → 检查ACR存在性 → 更新状态 → 记录结果
```

## 8. 配置管理

### 8.1 配置存储模型
```go
type SystemConfig struct {
    ID           uint   // 主键
    ConfigKey    string // 配置键(唯一)
    ConfigValue  string // 配置值
    Description  string // 描述
    IsEncrypted  bool   // 是否加密
    ConfigGroup  string // 配置分组
    DisplayOrder int    // 显示顺序
}
```

### 8.2 主要配置项
- **ACR配置**: registry地址、命名空间、认证信息
- **Git配置**: Gitee/GitHub仓库URL、认证信息、本地路径
- **同步策略**: 重试次数、超时时间、并发数
- **系统限制**: 队列大小、资源限制

## 9. 错误处理和日志

### 9.1 错误分类
- **配置错误**: Git配置缺失、认证失败
- **网络错误**: 仓库连接失败、API调用超时
- **同步错误**: 镜像拉取失败、推送失败
- **系统错误**: 数据库连接、磁盘空间

### 9.2 日志策略
- **结构化日志**: 使用zap进行日志记录
- **日志级别**: Debug、Info、Warn、Error
- **上下文信息**: 任务ID、镜像地址、错误详情

## 10. API接口设计

### 10.1 核心接口
```
POST /api/v1/sync/submit        # 单个镜像同步
POST /api/v1/sync/batch         # 批量镜像同步
GET  /api/v1/sync/status/:id    # 获取同步状态
GET  /api/v1/sync/history       # 同步历史记录
GET  /api/v1/images/list        # 镜像列表查询
```

### 10.2 响应格式
```json
{
    "task_id": "uuid",
    "status": "pending|running|completed|failed",
    "total_images": 10,
    "completed_images": 5,
    "progress": 50.0,
    "github_action_url": "https://github.com/...",
    "estimated_completion": "2024-01-01 12:00:00"
}
```

## 11. 性能优化

### 11.1 数据库优化
- **索引策略**: 状态字段、任务ID、创建时间
- **分页查询**: 避免大量数据加载
- **连接池**: 复用数据库连接

### 11.2 并发优化
- **异步处理**: 所有耗时操作异步执行
- **goroutine池**: 控制并发数量
- **channel通信**: 安全的数据传递

## 12. 安全机制

### 12.1 认证和授权
- **Token认证**: Git仓库访问令牌
- **加密存储**: 敏感配置信息加密
- **CORS保护**: 跨域请求控制

### 12.2 输入验证
- **参数验证**: 镜像地址格式检查
- **SQL注入防护**: 使用ORM预编译语句
- **XSS防护**: 输入输出转义

## 13. 监控和运维

### 13.1 健康检查
```go
// API健康检查
GET /api/v1/health

// 返回系统状态
{
    "status": "healthy",
    "timestamp": "2024-01-01T12:00:00Z",
    "services": {
        "database": "healthy",
        "git": "healthy",
        "github": "healthy"
    }
}
```

### 13.2 监控指标
- **任务统计**: 成功率、失败率、平均耗时
- **系统指标**: CPU、内存、磁盘使用率
- **业务指标**: 日同步量、存储增长趋势

---

## 总结

这个Docker镜像同步平台采用了现代化的微服务架构，具有以下特点：

1. **高度模块化**: 清晰的分层架构，职责分离
2. **灵活配置**: 支持动态配置和多仓库切换
3. **可靠性强**: 完善的重试机制和错误处理
4. **可扩展性**: 支持水平扩展和高并发处理
5. **监控完善**: 全面的状态监控和日志记录
6. **安全可靠**: 加密存储和权限控制

整个同步流程自动化程度高，从用户提交请求到最终镜像同步完成，实现了端到端的自动化处理，大大提升了运维效率。