# Docker 镜像同步平台 — 优化计划（2026-04-02 创建，04-03 更新）

> **04-03 第三次更新**：P0 全部完成（#1 ~ #5），P1 全部完成（#6 ~ #11）。
> 下一步可进入 P2 批次。

---

## P0：严重问题（需立即修复）

### ~~1. 后端路由顺序导致接口不可用~~ ✅ 已修复
> `main.go` 中 `/stats`、`/list`、`/batch-check` 已注册在 `/:id` 之前。

### ~~2. 读写锁使用错误（数据竞争）~~ ✅ 已修复
- **文件**：`internal/services/git_factory.go`
- **修复内容**：抽取 `getOrCreateGitService()` 和 `ensureOptimizedServiceInitialized()`，使用 double-check locking（先 RLock 检查→释放→Lock 写入并二次检查），消除读锁下写共享状态的数据竞争

### ~~3. ConfigService 缓存无互斥保护~~ ✅ 已修复
- **文件**：`internal/services/config.go`
- **修复内容**：`cache` 字段从 `map[string]string` 改为 `sync.Map`，所有读写操作改用 `Load`/`Store`/`Delete`，并发安全

### ~~4. 前端运行时错误~~ ✅ 已修复
- **ImagesView.vue**：import 改为 `getStatusType`/`getStatusText`（与模板一致，`status.js` 已导出）
- **AliyunConfigForm.vue**：import 补充 `CircleCheck` 图标组件

### ~~5. 安全隐患~~ ✅ 已修复
- ✅ 已增加认证/授权中间件（`middleware/auth.go`），业务接口挂在 `protected` 路由组上
- ✅ `/config/debug/:key` 调试路由改为仅在开发模式（`mode != "release"`）下注册
- ✅ 创建 `config.yaml.example`（敏感值用占位符），`config.yaml` 加入 `.gitignore`
- ✅ `encryption.go` 和 `database.go`：`GIN_MODE=release` 且未设 `ENCRYPTION_KEY` 时返回错误阻止启动

---

## P1：重要问题

### 6. 数据库模型与业务值不一致
- **文件**：`internal/models/models.go`（约 119 行）
- **现状**：`SyncTask.Status` 的 GORM enum 仍为 `pending/running/completed/failed/paused`，但业务常量含 `TaskStatusPartialSuccess = "partial_success"`，`sync.go` 约 1414 行会写入该状态
- **修复**：在 enum 中增加 `partial_success`

### 7. 批量同步无事务保护
- **文件**：`internal/handlers/sync.go`（约 156-217 行）
- **现状**：`SubmitBatchSync` 先 `Create(task)` 再循环 `Create(record)`，无事务包裹
- **修复**：用 `database.DB.Transaction()` 包裹整段创建逻辑

### 8. 镜像名解析不够健壮
- **文件**：`internal/handlers/sync.go`（约 1472-1479 行）
- **现状**：`parseImageNameAndTag` 和 `parseImageInfo` 都用简单 `Split(":")` 处理，带端口的 registry（如 `registry:5000/repo:tag`）会被截断错误
- **修复**：使用 `reference.ParseNormalizedNamed` 或按最后一个 `:` 拆分，并区分端口与 tag

### 9. 后台协程无优雅关闭
- **文件**：`main.go`
- **现状**：仅有 `signal + srv.Shutdown`，`sync.go` 中 `go processSyncTask()` 等后台协程无收尾逻辑
- **修复**：使用 `context.WithCancel` + `sync.WaitGroup` 管理后台协程

### 10. Gitee 测试假成功
- **文件**：`internal/handlers/config.go`（约 746-761 行）
- **现状**：`testGiteeConnection` 仍为占位函数，直接 `return nil`
- **修复**：实现真实的 Gitee API 连接测试（如请求 `https://gitee.com/api/v5/repos/{owner}/{repo}`）

### 11. CORS 配置矛盾
- **文件**：`internal/middleware/cors.go`（约 51-56 行）
- **现状**：仍同时设置 `Allow-Origin: *` 与 `Allow-Credentials: true`
- **修复**：生产环境使用白名单 origin（从配置读取），或移除 `Allow-Credentials: true`

---

## P2：中等优先级

### 12. 前端样式不一致 — 🔶 部分修复
- ✅ 已引入 `design-tokens.css`，部分页面使用 CSS 变量
- ❌ `SyncView` 统计数字 28px vs `ImagesView` 统计数字 32px，标签 13px vs 14px
- ❌ `ImagesView` 仍使用 `max-width: 1400px`，其他页面用 `var(--max-width)` (1440px)
- ❌ `ConfigView` 仍有「外卡片 + 灰色 tab 顶栏」的双层灰结构
- **修复**：统一使用 design-tokens 变量；ImagesView 改用 `var(--max-width)`

### 13. 响应式缺失
- **现状**：全部页面未使用 Element Plus 响应式栅格（`:xs/:sm/:md`）；导航栏无移动端折叠菜单
- **修复**：使用 Element Plus 响应式 span；导航增加移动端 `el-drawer`

### 14. GitHub 列表分页 + 过滤逻辑矛盾
- **文件**：`web/src/views/GitHubView.vue`
- **现状**：服务端分页 + 客户端过滤（`filteredRuns` 只对当前页数据过滤），`totalRuns` 为服务端总数与表格行数不一致
- **修复**：筛选交给 API 参数，或改为全量前端列表+前端分页

### 15. Docker 容器安全
- **现状**：`Dockerfile-all` 创建了 `appuser` 但 `supervisord.conf` 中 `user=root`；Compose 无资源限制
- **修复**：supervisord 中 backend 改为 `user=appuser`；Compose 添加 `deploy.resources.limits`

### 16. 配置结构体缺字段
- **文件**：`internal/config/config.go`（约 64-71 行）
- **现状**：顶层 `Config` 仍缺 `Security`、`GitHubActions` 子结构体，`config.yaml` 中对应配置段被忽略
- **修复**：补充结构体字段并在代码中使用

### 17. 前端交互问题 — 🔶 部分修复
- ✅ `SyncView` 刷新已改为静默（无 success 提示）
- ❌ `ImagesView` 刷新仍弹 `ElMessage.success('数据已刷新')`（约 419 行）
- ❌ `BatchSyncForm` 输入校验无防抖，每次 `@input` 直接校验并弹错误
- ❌ 批量同步「清空」无二次确认
- ❌ `ImagesView` 的 `searchTimer` 未在 `onUnmounted` 中清除
- **修复**：ImagesView 刷新改静默；校验增加 debounce；清空加确认弹窗；补充 clearTimeout

---

## P3：低优先级

### 18. 缺失 CI/CD
- **现状**：仓库内无 `.github/workflows/` 目录
- **修复**：添加 GitHub Actions（lint + build）

### 19. 无自动化测试
- **现状**：仓库内无 `*_test.go`；前端 `package.json` 无 test 脚本
- **修复**：逐步补充核心业务的单元测试

### 20. 文档不一致 — 🔶 部分修复
- ✅ `go.mod` Go 版本与 README 一致（1.21）
- ✅ `vite.config.js` 端口 3000 与实际一致
- ❌ `docs/README.md` 引用的 `configuration.md`/`api.md`/`deployment.md`/`troubleshooting.md` 均不存在
- ❌ 根 `README.md` 引用 `cp .env.example .env`，仓库中无此文件
- ❌ `Makefile` 声明了 `docker-build` 等 `.PHONY` 目标但未实现
- **修复**：删除断裂引用或补充对应文件；Makefile 补充或清理未实现的目标

### 21. 代码重复
- **现状**：
  - `SyncView` 统计区已用 `v-for` 驱动，但 `ImagesView` 仍为 4 个手写 `el-card`
  - `BatchSyncForm` 中 `submitBatchSync` 与 `submitMockBatchSync`（约 634-849 行）仍有大段重复的 payload 构建逻辑
- **修复**：抽取公共组件 `StatsCards.vue`；提取公共函数 `buildBatchPayload()`

### 22. 日志与错误格式不统一 — 🔶 部分修复
- ✅ 主体已使用 `logger.Logger` + `zap`
- ❌ `main.go` 仍有 `log.Fatalf`；`database.go`/`config/config.go` 有 `log.Println`
- ❌ `handlers/config.go` 阿里云测试路径大量 `fmt.Printf`
- ❌ API 错误响应格式不统一：有 `gin.H{"error": ...}` 也有 `status + message` 形式
- **修复**：全局替换为 zap；统一错误响应结构体

### 23. 前端工程化 — 🔶 部分修复
- ✅ `package.json` 有 `lint` 脚本和 ESLint 依赖
- ❌ 仓库中无 `.eslintrc*` 或 `eslint.config.*` 配置文件，规则未固化
- ❌ 无 test 脚本
- ❌ `vite` 4.x / `vue` 3.3 版本偏旧
- **修复**：添加 ESLint 配置文件；拆分 `lint` 与 `lint:fix`；评估依赖升级

### 24. 前端死代码 / 路由问题 — 🔶 部分修复
- ✅ `stores/image.js` 中 `console.log`/`console.trace` 已清理
- ❌ `SyncView.vue` 中跳转 `/images`，但 `router/index.js` 无此路由
- ❌ `ImagesView` 约 399 行仍用原生 `fetch('/api/v1/config/aliyun')` 而非 axios
- **修复**：补充 `/images` 路由或改为跳转已有页面；统一使用 axios

---

## 状态跟踪

| 编号 | 优先级 | 状态 | 备注 |
|------|--------|------|------|
| 1 | P0 | ✅ 已修复 | 路由顺序已调整 |
| 2 | P0 | ✅ 已修复 | double-check locking 消除读锁下写入 |
| 3 | P0 | ✅ 已修复 | cache 改为 sync.Map |
| 4 | P0 | ✅ 已修复 | import 函数名统一 + CircleCheck 补充导入 |
| 5 | P0 | ✅ 已修复 | debug 路由限制开发环境；config.yaml 模板化；默认密钥生产环境报错 |
| 6 | P1 | ✅ 已修复 | enum 增加 partial_success |
| 7 | P1 | ✅ 已修复 | SubmitBatchSync/SubmitSync 改为 DB.Transaction() |
| 8 | P1 | ✅ 已修复 | splitImageAndTag 按最后一个 : 拆分，区分端口与 tag |
| 9 | P1 | ✅ 已修复 | SyncHandler 持有 ctx/wg，Shutdown() 等待协程完成 |
| 10 | P1 | ✅ 已修复 | 调用 Gitee API v5 /repos/{owner}/{repo} 验证 |
| 11 | P1 | ✅ 已修复 | 有 Origin 时回显实际值+credentials；无 Origin 时用 * 不设 credentials |
| 12 | P2 | 🔶 部分 | 字号/宽度仍不一致 |
| 13 | P2 | ❌ 待处理 | 无响应式 + 无移动端导航 |
| 14 | P2 | ❌ 待处理 | 分页+过滤矛盾 |
| 15 | P2 | ❌ 待处理 | Docker 仍以 root 运行 |
| 16 | P2 | ❌ 待处理 | 结构体缺字段 |
| 17 | P2 | 🔶 部分 | 部分交互已改善 |
| 18 | P3 | ❌ 待处理 | 无 CI/CD |
| 19 | P3 | ❌ 待处理 | 无测试 |
| 20 | P3 | 🔶 部分 | Go/端口已一致；文档引用仍断裂 |
| 21 | P3 | ❌ 待处理 | ImagesView 统计卡/BatchSync 重复 |
| 22 | P3 | 🔶 部分 | 主体 zap；混有 log/fmt |
| 23 | P3 | 🔶 部分 | 有依赖无配置文件 |
| 24 | P3 | 🔶 部分 | console 已清；路由/fetch 待修 |

### 建议执行顺序

1. ~~**第一批（P0 剩余）**：#2 读写锁 → #3 缓存锁 → #4 前端函数名 → #5 剩余安全项~~ ✅ 已完成
2. ~~**第二批（P1）**：#6 enum → #7 事务 → #8 镜像解析 → #11 CORS~~ ✅ 已完成
3. ~~**第三批（P1 低风险）**：#10 Gitee → #9 优雅关闭~~ ✅ 已完成
4. **第四批（P2 高价值）**：#17 前端交互 → #16 配置结构体
4. **第四批（P2 剩余）**：#12 样式 → #14 分页 → #13 响应式 → #15 Docker
5. **第五批（P3）**：按需推进
