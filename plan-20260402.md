# Docker 镜像同步平台 — 优化计划（2026-04-02）

## P0：严重问题（需立即修复）

### 1. 后端路由顺序导致接口不可用
- **文件**：`main.go` 230-242 行
- **问题**：`/:id` 注册在 `/stats` 之前，Gin 按顺序匹配，导致 `/images/stats` 被 `/:id` 拦截，`id="stats"` 解析失败返回 400
- **修复**：将 `/stats`、`/list`、`/batch-check` 等固定路径全部写在 `/:id` 之前

### 2. 读写锁使用错误（数据竞争）
- **文件**：`internal/services/git_factory.go` 71-83、233-241 行
- **问题**：在 `RLock`（读锁）下对共享字段赋值，违反读写锁语义，存在并发数据竞争
- **修复**：懒初始化改为 `Lock()`，或使用 `sync.Once`

### 3. ConfigService 缓存无互斥保护
- **文件**：`internal/services/config.go`
- **问题**：`cache map[string]string` 在多个方法中并发读写无锁保护，Go 的 map 并发读写会直接 panic
- **修复**：为 cache 增加 `sync.RWMutex` 或改用 `sync.Map`

### 4. 前端运行时错误
- **文件**：`web/src/views/ImagesView.vue` 192-193、310-311、369 行
  - 模板调用 `getStatusType`/`getStatusText`，但 import 的是 `getSyncStatusType`/`getSyncStatusText`，渲染时会报未定义错误
- **文件**：`web/src/components/AliyunConfigForm.vue` 111、120 行
  - 模板使用 `<CircleCheck />`，但 script 中未导入该图标组件

### 5. 安全隐患
- 全 API 无认证/授权：任意客户端可改配置、触发同步、读取敏感调试信息
- `GET /config/debug/:key` 相当于调试后门，可按 key 读取配置明文
- `config.yaml` 和 `docker-compose` 文件中包含真实密码、GitHub PAT、阿里云密码等明文凭据
- 加密服务未设 `ENCRYPTION_KEY` 环境变量时使用可预测的默认密钥

---

## P1：重要问题

### 6. 数据库模型与业务值不一致
- **文件**：`internal/models/models.go` 119 行
- **问题**：`SyncTask.Status` 的 GORM enum 定义为 `pending/running/completed/failed/paused`，但业务代码中使用了 `partial_success`，MySQL strict 模式下写入会被拒绝
- **修复**：在 enum 中增加 `partial_success`

### 7. 批量同步无事务保护
- **文件**：`internal/handlers/sync.go` 157-217 行
- **问题**：`SubmitBatchSync` 循环 `Create` 记录，中途失败返回 500，但之前已创建的 task 和部分 records 未回滚
- **修复**：用 `database.DB.Transaction()` 包裹

### 8. 镜像名解析不够健壮
- **文件**：`internal/handlers/sync.go` 1459-1479 行
- **问题**：`parseImageNameAndTag` 用简单 `Split(":")` 处理，带端口的 registry 地址如 `registry:5000/repo:tag` 会被截断错误
- **修复**：使用 `reference.ParseNormalizedNamed` 或更健壮的解析逻辑

### 9. 后台协程无优雅关闭
- **文件**：`main.go`
- **问题**：`signal + srv.Shutdown` 已实现，但未等待后台 `go processSyncTask` / `monitorGitHubActions` 完成
- **修复**：使用 `context.WithCancel` + `sync.WaitGroup` 管理后台协程

### 10. Gitee 测试假成功
- **文件**：`internal/services/config.go` 755-761 行
- **问题**：`testGiteeConnection` 直接 `return nil`，Gitee 测试永远成功，误导用户
- **修复**：实现真实的 Gitee API 连接测试

### 11. CORS 配置矛盾
- **文件**：`internal/middleware/cors.go` 46-56 行
- **问题**：`Allow-Origin: *` 与 `Allow-Credentials: true` 同时设置不合 CORS 规范
- **修复**：生产环境使用白名单 origin

---

## P2：中等优先级

### 12. 前端样式不一致
- 统计卡片字号不一致：`SyncView` 28px vs `ImagesView` 32px
- 最大宽度不一致：`App.vue` header 1200px vs 各页面内容 1400px
- `ConfigView` 灰底 + 白卡片叠加 `App.vue` 灰底形成"双层灰"
- **修复**：抽取共享 SCSS 变量或 CSS Variables，统一设计令牌

### 13. 响应式缺失
- 所有页面筛选区使用固定 `span`，窄屏严重挤压
- 导航栏 `horizontal` 模式小屏溢出，无折叠/抽屉菜单
- `GitHubView` 四列状态卡片手机端可读性差
- **修复**：使用 Element Plus 的 `:xs/:sm/:md` 响应式 span，导航增加移动端抽屉

### 14. GitHub 列表分页 + 过滤逻辑矛盾
- **文件**：`web/src/views/GitHubView.vue` 310-354 行
- **问题**：服务端分页 + 客户端再过滤，totalRuns 为服务端总数，表格数据却是当前页再过滤结果
- **修复**：筛选交给 API 参数，或改为全量前端列表

### 15. Docker 容器安全
- 后端容器以 root 运行
- `Dockerfile-all` 中 `adduser appuser` 未实际生效
- Docker Compose 资源限制全部注释掉
- **修复**：确保运行用户为非 root，取消注释资源限制

### 16. 配置结构体缺字段
- **文件**：`internal/config/config.go`
- **问题**：`security`、`github_actions`、连接池参数在 YAML 中有但未映射到 Go 结构体，写了等于没写
- **修复**：补充结构体字段并在代码中使用

### 17. 前端交互问题
- 每次刷新列表成功都弹 `ElMessage.success`，频繁操作打扰用户
- `BatchSyncForm` 输入过程中每次校验失败立即弹错误提示，造成刷屏
- 批量同步"清空"镜像列表无二次确认
- `searchTimer` 未在 `onUnmounted` 中清除
- **修复**：刷新成功改为静默或轻量提示；校验错误防抖；清空增加确认；清除定时器

---

## P3：低优先级

### 18. 缺失 CI/CD
- `.github/` 目录不存在，无自动化工作流
- **修复**：添加 GitHub Actions（lint + test + build + deploy）

### 19. 无自动化测试
- 仓库内无 `*_test.go`，前端无 test 脚本
- **修复**：逐步补充核心业务的单元测试和 E2E 测试

### 20. 文档不一致
- `docs/README.md` 引用的 `configuration.md`/`api.md`/`deployment.md`/`troubleshooting.md` 均不存在
- 根 `README.md` 引用 `cp .env.example .env`，仓库中无此文件
- `go.mod` 写 go 1.19，README 写 Go 1.21+
- `web/vite.config.js` 端口 3000，README 写 5173
- 根 Makefile 声明了 `docker-build` 等目标但未实现
- **修复**：统一文档与实际配置

### 21. 代码重复
- `SyncView` 与 `ImagesView` 的统计卡、筛选行、表格列、详情弹窗高度重复
- `BatchSyncForm` 中 `submitBatchSync` 与 `submitMockBatchSync` 构建 payload 逻辑重复
- **修复**：抽取公共组件 `ImageListPanel.vue` 和公共函数 `buildBatchPayload()`

### 22. 日志与错误格式不统一
- 后端混用 zap、log、fmt.Printf
- 错误响应：config 返回英文 `status/message`，sync/image 返回中文 `error`
- `middleware/error.go` 提供的 `HandleError`/`APIError` 在 handler 中几乎未使用
- **修复**：统一使用 zap；统一错误响应结构体

### 23. 前端工程化
- 无 ESLint 配置文件，规则未固化
- `package.json` 中 `lint` 脚本自带 `--fix`，CI 中会意外改文件
- Vite/Vue 版本偏旧（vite 4.x, vue 3.3）
- **修复**：添加 `.eslintrc.js`；拆分 `lint` 与 `lint:fix`；评估升级

### 24. 前端死代码 / 路由问题
- `SyncView.vue` 中 `viewSyncDetails` 跳转 `/images`，但 router 中无此路由
- `ImagesView` 用原生 `fetch` 而非 axios 实例
- `ConfigView` 中有未在模板使用的 Git 相关函数（死代码）
- `stores/image.js` 中残留 `console.log`/`console.trace`
- **修复**：删除死代码，统一 API 调用方式，清理控制台日志

---

## 状态跟踪

| 编号 | 状态 | 备注 |
|------|------|------|
| 1-24 | ⬜ 待处理 | |
