## 项目概述
**目标**：构建一个网站，用于同步Docker镜像到阿里云ACR，并管理同步记录。

## 功能模块

### 1. 手动同步界面
- **镜像输入**：
  - 支持输入多个镜像名称和tag（每行一个）
  - 支持私库镜像（如 `k8s.gcr.io/kube-state-metrics/kube-state-metrics:v2.0.0`）
  - 支持指定架构（如 `--platform=linux/arm64 xiaoyaliu/alist`）

- **同步流程**：
  - 点击同步按钮后，将新输入的镜像追加到 `images.txt`
  - 自动注释掉之前的所有内容（每行前加 `#`）
  - 创建git commit并推送到Gitee
  - 等待代码自动同步到GitHub
  - 监控GitHub仓库更新状态

### 2. GitHub Action集成
- 使用现有的 `.github/workflows/docker.yaml`
- 监控Action执行状态
- Action成功执行后，镜像会同步到阿里云ACR

### 3. 结果展示
- **同步结果**：
  - 展示同步后的阿里云ACR地址
  - 提供 `docker pull` 命令复制按钮（不直接显示命令文本）
  
- **数据库存储**：
  - 将镜像名称、tag、阿里云ACR地址存入MySQL数据库
  - 使用免费MySQL数据库服务

### 4. 镜像列表界面
- 从MySQL数据库中读取所有同步记录
- 展示所有已同步的镜像信息

## 核心流程
```
用户输入 → 更新images.txt → Gitee提交 → GitHub同步 → 
Action执行 → 数据库存储 → 结果展示
```

## 完整技术栈设计

### 后端技术栈
- **语言**: Go 1.19+
- **Web框架**: Gin (轻量高效)
- **数据库ORM**: GORM (MySQL)
- **配置管理**: Viper
- **Git操作**: go-git 或调用git命令行
- **API调用**: 内置net/http 或 resty
- **任务队列**: 内置goroutine 或 asynq
- **日志**: Zap 或 logrus

### 前端技术栈
- **框架**: Vue 3 + Vite
- **UI组件**: Element Plus
- **状态管理**: Pinia
- **HTTP客户端**: Axios
- **构建工具**: Vite
- **语言**: TypeScript + Composition API

### 数据库
- **MySQL免费方案**:
  - PlanetScale (Serverless MySQL)
  - Railway 提供的免费MySQL
  - Neon (PostgreSQL, 如果可接受)
  - 阿里云/腾讯云免费试用

### 基础设施
- **版本控制**: Gitee + GitHub 同步
- **CI/CD**: GitHub Actions (已有)
- **部署**: 可考虑 Vercel (前端) + Railway/Render (后端)
- **监控**: 简单的健康检查接口

## 系统架构设计

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   前端界面      │    │    Go后端API     │    │    MySQL数据库  │
│                 │    │                  │    │                 │
│ - Vue 3         │◄──►│ - Gin框架        │◄──►│ - 镜像记录表    │
│ - Element Plus  │    │ - GORM          │    │ - 同步日志表    │
│ - Axios         │    │ - 任务调度       │    │                 │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                                │
                                │
                                ▼
                        ┌──────────────────┐
                        │   外部服务集成    │
                        │                  │
                        │ - Gitee API      │
                        │ - GitHub API     │
                        │ - GitHub Actions │
                        │ - 阿里云ACR      │
                        └──────────────────┘
```

## 核心模块设计

### 后端模块
1. **Web Server** (Gin) - HTTP API
2. **Git操作模块** - 操作images.txt文件
3. **任务调度模块** - 监控同步状态
4. **数据库模块** - 镜像记录CRUD
5. **外部API模块** - Gitee/GitHub/ACR接口

### 前端模块
1. **同步界面** - 镜像输入和同步控制
2. **镜像列表** - 数据展示和搜索
3. **状态监控** - 实时显示同步进度
4. **结果展示** - ACR地址和复制功能

## 数据库表设计

```sql
-- 镜像同步记录表
CREATE TABLE image_sync_records (
    id INT AUTO_INCREMENT PRIMARY KEY,
    original_image VARCHAR(500) NOT NULL,      -- 原始镜像地址
    acr_image VARCHAR(500),                    -- 阿里云ACR地址
    tag VARCHAR(100),                         -- 标签
    architecture VARCHAR(50),                 -- 架构
    sync_status ENUM('pending', 'syncing', 'success', 'failed'),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 同步任务表  
CREATE TABLE sync_tasks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    task_id VARCHAR(100) UNIQUE,              -- 任务ID
    images_json TEXT,                         -- 本次同步的镜像列表
    status ENUM('pending', 'running', 'completed', 'failed'),
    github_action_url VARCHAR(500),           -- GitHub Action链接
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## API接口设计

### 同步相关
- `POST /api/sync` - 提交同步任务
- `GET /api/sync/status/:taskId` - 获取同步状态
- `GET /api/sync/history` - 同步历史

### 镜像管理
- `GET /api/images` - 获取镜像列表
- `GET /api/images/:id` - 获取镜像详情
- `DELETE /api/images/:id` - 删除镜像记录

## 开发计划

### Phase 1: 基础框架
- Go后端基础框架搭建
- 数据库连接和表创建
- 前端Vue3项目初始化

### Phase 2: 核心功能
- 镜像同步提交功能
- Git操作模块
- 基础前端界面

### Phase 3: 状态监控
- GitHub Actions状态监控
- 实时状态更新
- 结果展示

### Phase 4: 完善优化
- 错误处理和完善
- 界面美化
- 部署配置

这个技术栈方案你觉得怎么样？需要调整哪些部分？