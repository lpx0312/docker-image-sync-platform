# 用户权限管理功能实施计划

> 日期：2026-04-03  
> 状态：已完成

## 1. 需求概述

为 Docker 镜像同步平台增加基于角色的菜单权限管理功能。管理员可以在创建/编辑用户时分配角色，不同角色看到不同菜单、访问不同 API。

### 核心需求
- 普通用户（`user`）仅能看到「镜像同步」
- 运维员（`operator`）可看到「镜像同步」+「GitHub Actions」+「系统配置」
- 管理员（`admin`）可看到所有菜单，包括「用户管理」
- 后端 API 也需同步拦截，无权限的角色调用对应 API 返回 403

## 2. 角色与权限矩阵

| 角色 | 标识 | 镜像同步 (sync) | GitHub Actions (github) | 系统配置 (config) | 用户管理 (users) |
|------|------|:---:|:---:|:---:|:---:|
| 管理员 | `admin` | ✅ | ✅ | ✅ | ✅ |
| 运维员 | `operator` | ✅ | ✅ | ✅ | ❌ |
| 普通用户 | `user` | ✅ | ❌ | ❌ | ❌ |

### 权限标识与 API 路由映射

| 权限标识 | 菜单 | API 路由 |
|----------|------|---------|
| `sync` | 镜像同步 | `/sync/*`, `/images/*` |
| `github` | GitHub Actions | `/github/*` |
| `config` | 系统配置 | `/config/*` |
| `users` | 用户管理 | `/auth/users*`, `/auth/login-logs` |

## 3. 技术方案

### 3.1 设计原则
- **复用现有 `Role` 字段**：不新增数据库表，通过 User 模型的 `Role` 字段映射权限
- **后端代码定义权限映射**：角色→权限的关系在 Go 代码中硬编码，修改角色权限只需改代码
- **前后端双重校验**：前端隐藏无权限菜单 + 后端中间件拦截无权限 API 请求

### 3.2 不改动的部分
- 现有 `admin` / `user` 角色标识保持兼容
- 现有的 `AuthRequired` 和 `AdminRequired` 中间件保留
- 登录/认证流程不变

## 4. 改动清单

### 4.1 后端改动

#### (A) `internal/models/models.go`
- 新增角色常量：`RoleAdmin`, `RoleOperator`, `RoleUser`
- 新增角色权限映射函数 `GetRolePermissions(role string) []string`
- 新增角色列表函数 `GetAllRoles()` 返回角色信息（标识、名称、权限列表）
- 新增权限常量：`PermSync`, `PermGitHub`, `PermConfig`, `PermUsers`

#### (B) `internal/middleware/auth.go`
- 新增 `PermissionRequired(permission string)` 中间件
- 从 JWT claims 中获取用户角色，查询角色权限，判断是否包含所需权限
- 无权限时返回 HTTP 403 + JSON 错误响应

#### (C) `internal/handlers/auth.go`
- 新增 `GetRoles` 处理器：`GET /auth/roles` 返回可用角色列表及其权限
- 修改 `GetCurrentUser` / `Login` 响应：在用户信息中附加 `permissions` 字段
- 修改 `CreateUser`：验证 role 值必须是合法角色（admin/operator/user）
- 修改 `UpdateUser`（如有）：支持修改用户角色

#### (D) `main.go`
- 给 `/config` 路由组添加 `PermissionRequired("config")`
- 给 `/github` 路由组添加 `PermissionRequired("github")`
- `/sync` 和 `/images` 保持仅需 `AuthRequired`（所有角色都有 sync 权限）
- 管理员路由保持 `AdminRequired`（`users` 权限由 admin 角色隐含）

### 4.2 前端改动

#### (E) `web/src/stores/auth.js`
- 新增 `permissions` 状态字段（从登录响应中获取）
- 新增 `hasPermission(perm)` 方法
- 修改 `login` action：保存 permissions 到 store 和 storage

#### (F) `web/src/App.vue`
- 修改 `navItems` computed：根据 `authStore.hasPermission()` 过滤菜单项
- 给每个菜单项增加 `permission` 属性用于过滤

#### (G) `web/src/router/index.js`
- 路由 meta 中增加 `requiredPermission` 字段
- `beforeEach` 守卫中增加权限校验：无权限则重定向到 `/sync`

#### (H) `web/src/views/UserManageView.vue`
- 创建用户对话框：将 role 改为下拉选择器（admin/operator/user）
- 增加权限预览：选择角色后显示该角色拥有的菜单权限
- 用户列表中显示角色标签（不同颜色区分）

## 5. 实施步骤

| 步骤 | 说明 | 文件 |
|------|------|------|
| 1 | 后端：定义角色常量和权限映射 | `internal/models/models.go` |
| 2 | 后端：实现 `PermissionRequired` 中间件 | `internal/middleware/auth.go` |
| 3 | 后端：新增角色 API + 修改用户响应 | `internal/handlers/auth.go` |
| 4 | 后端：路由绑定权限中间件 | `main.go` |
| 5 | 前端：auth store 增加权限管理 | `web/src/stores/auth.js` |
| 6 | 前端：菜单和路由增加权限过滤 | `web/src/App.vue`, `web/src/router/index.js` |
| 7 | 前端：用户管理页增加角色选择器 | `web/src/views/UserManageView.vue` |
| 8 | 测试验证 | 各角色登录验证菜单和 API 权限 |

## 6. 兼容性说明

- 现有 `admin` 角色用户不受影响（权限最大）
- 现有 `user` 角色用户登录后仅能看到「镜像同步」菜单
- 新增 `operator` 角色需管理员手动分配
- 数据库无需迁移，`Role` 字段已存在
